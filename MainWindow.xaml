<Window x:Class="smartClass.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:smartClass"
        xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        ui:TitleBar.Height="36"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Name="SettingsBtn" Content="设置" Width="80" Margin="0,0,10,0"/>
            <Button Name="ExitBtn" Content="退出" Width="80"/>
        </StackPanel>

        <ikw:SimpleStackPanel Grid.Row="1" VerticalAlignment="Center" Spacing="5">
            <TextBlock Text="智能教室系统" FontSize="24" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock Text="Made with love by 雨夜漫步" FontSize="20" TextAlignment="Center"/>

        </ikw:SimpleStackPanel>

        <StatusBar Grid.Row="2">
            <TextBlock Name="StatusText" Text="就绪" Margin="6,0"/>
        </StatusBar>
    </Grid>
</Window>
