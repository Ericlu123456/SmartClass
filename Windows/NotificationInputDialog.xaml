<Window x:Class="smartClass.Windows.NotificationInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        Title="添加提醒" Height="220" Width="420" WindowStartupLocation="CenterScreen"
        Topmost="True" ShowInTaskbar="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="提醒内容:" FontWeight="Bold" FontSize="14"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

        <TextBox Name="ContentTextBox" Grid.Row="1"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" Margin="0,8"
                 FontSize="14"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button Name="OkBtn" Content="确认" Width="80" Margin="10,0"/>
            <Button Name="CancelBtn" Content="取消" Width="80" Margin="10,0"/>
        </StackPanel>
    </Grid>
</Window>
