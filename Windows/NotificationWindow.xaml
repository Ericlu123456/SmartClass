<Window x:Class="smartClass.Windows.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="提醒"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">

    <Border Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
        <Grid MaxWidth="400" MinWidth="200">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 标题 -->
            <TextBlock Text="提醒" FontWeight="Bold" Grid.Row="0" Grid.Column="0"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

            <!-- 关闭按钮 -->
            <Button Name="CloseBtn" Grid.Row="0" Grid.Column="1"
                    Content="✕" Width="24" Height="24" Padding="0"
                    VerticalAlignment="Top" Cursor="Hand"
                    Background="Transparent" BorderThickness="0"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>

            <!-- 通知内容 -->
            <TextBlock Name="ContentText" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                       TextWrapping="Wrap" Margin="0,8,0,0"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
        </Grid>
    </Border>
</Window>
